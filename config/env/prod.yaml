environment: "production"

database:
    host: "db.production.local"
    port: 5432
    name: "robot_prod"
    user: "prod_user"

redis:
    host: "redis.production.local"
    port: 6379

logging:
    level: "INFO"
